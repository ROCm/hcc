# temporary mark this set of tests as unsupport
# need to fix them for HSA 1.0F
config.unsupported = True

#import platform
#if config.root.target_triple == platform.system():
#    config.unsupported = True
